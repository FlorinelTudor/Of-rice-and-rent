{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/tunnel?path=$1"
    }
  ],
  "buildCommand": "PUBLIC_URL= npm run build",
  "devCommand": "npm run start",
  "outputDirectory": "build"
}
